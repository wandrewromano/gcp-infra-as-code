# TODO: import {
#   to = google_storage_bucket.imported
#   id = "${var.project_id}-imported-demo"
# }
# Unlike a backend block, `import` blocks are evaluated after
# variables are resolved, so var.project_id works fine here.
